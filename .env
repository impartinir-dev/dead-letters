# Public URL the app is served from (with trailing slash). Social cards need
# absolute image URLs, so index.html builds og:image / og:url from this.
# Change it if you move to a custom domain.
VITE_SITE_URL=https://impartinir-dev.github.io/dead-letters/
